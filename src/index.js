import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// render the instance of App to html div element with ID equal to root, which sits at index.html.
ReactDOM.render(<App />,
  document.getElementById('root')
);


 